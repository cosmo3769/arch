" Disable vi compatibility to reduce unexpected
set nocompatible

" Show line numbers
set number

" Wrap the text according to the screen size
set wrap

" To see the status line below in vim
set laststatus=2

" To scroll when it reaches last 20th line
set scrolloff=20

" Case insensitive search
set ignorecase
set smartcase

" Turn syntax highlighting on
syntax on
